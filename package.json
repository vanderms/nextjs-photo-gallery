{
  "name": "net-ninja",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export && touch ./out/.nojekyll && python convert.py",
    "start": "next start",
    "deploy": "git subtree push --prefix out origin gh-pages"
    
  },
  "dependencies": {
    "axios": "^0.24.0",
    "next": "12.0.7",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "sass": "^1.45.1"
  }
}
